% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaleL.R
\name{scaleL}
\alias{scaleL}
\title{Bayesian imputation of Likert scale length for meta-analysis}
\usage{
scaleL(
  data,
  prior = "tier1",
  custom_prior = NULL,
  instrument_priors = NULL,
  L_grid = 4:12,
  lik_method = c("empirical", "full", "profile"),
  emp_R = 20000L,
  candidate_origins = NULL,
  M = 50L,
  method = c("MI", "modal", "bayes_point"),
  compute_meta = FALSE,
  effect_col = NULL,
  effect_se_col = NULL,
  tau2_method = "PM",
  min_cluster_size = 2L,
  min_cluster_modal_prob = 0.5,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data.frame with one row per measure. Required columns:
\code{study_id}, \code{mean}, \code{sd}, \code{n}. Optional: \code{L} (NA where missing),
\code{scale_min} (default 1; set to \code{NA} to trigger joint origin + L
imputation), \code{measure_id}, \code{instrument}, \code{true_L}, and the
composite-correction columns \code{n_items} / \code{alpha} / \code{inter_item_r}.}

\item{prior}{Prior specification: \code{"tier1"}, \code{"tier1_shifted"},
\code{"corpus"} (Laplace-smoothed observed-L frequencies),
\code{"field:<name>"} (where \verb{<name>} is one of the entries in
\link{FIELD_PRIORS}), or \code{"custom"}. Default \code{"tier1"}.}

\item{custom_prior}{Named numeric vector when \code{prior = "custom"}.}

\item{instrument_priors}{Optional named list of Tier 3 instrument-specific
priors. Studies whose \code{instrument} matches a name in this list use the
matching prior; others fall back to \code{prior}.}

\item{L_grid}{Integer vector of candidate scale lengths. Default \code{4:12}.}

\item{lik_method}{Likelihood method: \code{"empirical"} (default; Monte-Carlo
reference-histogram lookup, always available), \code{"full"} (2-D quadrature),
or \code{"profile"} (fast feasibility screen).}

\item{emp_R}{Number of synthetic studies per (L, N) cell for the empirical
reference. Lower it (e.g. 2000) for fast examples/tests.}

\item{candidate_origins}{Optional candidate origin values for origin
imputation. Default: sorted unique observed \code{scale_min} among known-L
studies, else \code{c(0, 1)}.}

\item{M}{Number of multiple imputations. Default 50.}

\item{method}{Reserved for future use. Currently the returned object
contains MI, modal, and bayes-point quantities; the method argument
only controls which is highlighted by \code{print()} (default \code{"MI"}).}

\item{compute_meta}{Logical; if \code{TRUE}, also fits a random-effects
meta-analysis (requires the \code{metafor} package).}

\item{effect_col, effect_se_col}{Optional column names: when supplied,
meta-analytic pooling is done on this external effect column instead of
the standardized mean. Imputation of L is still performed for use
downstream.}

\item{tau2_method}{Heterogeneity estimator passed to \code{metafor::rma}.
Default \code{"PM"} (Paule-Mandel).}

\item{min_cluster_size}{Minimum number of measures whose per-measure
modal-L posteriors agree before they are pooled into a joint block.
Default \code{2L}.}

\item{min_cluster_modal_prob}{Minimum per-measure modal posterior
probability required for a measure to be eligible for pooling with
other measures sharing its modal L. Default \code{0.5}.}

\item{seed}{Optional integer seed.}

\item{verbose}{Logical; emit progress messages.}
}
\value{
An S3 object of class \code{"scaleL"} with elements \code{data}, \code{studies},
\code{practical}, \code{diagnostic}, \code{meta} (if \code{compute_meta = TRUE}), \code{call},
\code{prior_used}, \code{L_grid}, \code{M}, \code{method}.
}
\description{
Main user-facing entry point. Takes a per-measure data.frame, computes
the posterior over candidate scale lengths L for each study, produces
multiple imputations and per-measure standardized means with calibrated
SEs, and (optionally) pools the result with random-effects meta-analysis
using Rubin's rules.
}
\section{Within-study L clustering}{

Each measure's per-measure posterior over L is computed independently.
Measures whose modal L agrees (with modal probability at least
\code{min_cluster_modal_prob}) are grouped; groups of at least
\code{min_cluster_size} measures form a pooled block and are imputed
jointly. Remaining measures are imputed as singletons. The per-measure
output \code{pooled_flag} indicates which measures were pooled.
}

\examples{
d <- read.csv(system.file("extdata", "sample_data.csv", package = "scaleL"))
\donttest{
# Empirical likelihood (default); shrink emp_R to keep the example fast.
fit <- scaleL(d, prior = "tier1", M = 10, emp_R = 2000, compute_meta = FALSE)
print(fit)
}
# Profile likelihood needs no Monte-Carlo reference:
fit2 <- scaleL(d, prior = "tier1", M = 10, lik_method = "profile")
}
