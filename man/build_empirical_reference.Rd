% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{build_empirical_reference}
\alias{build_empirical_reference}
\title{Build the empirical reference distribution for the empirical likelihood}
\usage{
build_empirical_reference(
  L_grid = 4:12,
  N_grid = EMP_N_GRID,
  R = EMP_R,
  nbins = EMP_NBINS,
  laplace = EMP_LAPLACE,
  rho_a = EMP_RHO_A,
  rho_b = EMP_RHO_B,
  mu_lo = EMP_MU_LO,
  mu_hi = EMP_MU_HI,
  scale_min = 1,
  verbose = FALSE
)
}
\arguments{
\item{L_grid}{Integer vector of candidate L values.}

\item{N_grid}{Anchor sample sizes for the histograms.}

\item{R}{Number of synthetic studies per (L, N) cell. Lower values build
faster (use for examples/tests); the default matches the manuscript.}

\item{nbins}{Histogram resolution per axis.}

\item{laplace}{Laplace smoothing pseudocount per bin.}

\item{rho_a, rho_b}{Beta parameters for the simulated intraclass correlation.}

\item{mu_lo, mu_hi}{Range of the simulated standardized mean.}

\item{scale_min}{Lowest scale value (default 1).}

\item{verbose}{Logical; print progress.}
}
\value{
A list keyed by candidate L (as character); each entry holds a
list of per-N log-density histograms.
}
\description{
Builds, by internal Monte-Carlo simulation (beta-binomial draws via
\code{\link[extraDistr:BetaBinom]{extraDistr::rbbinom()}}), a reference set of 2-D histograms over
\verb{(xbar, s^2)} for each candidate L at a grid of anchor sample sizes. The
reference does NOT depend on the user's corpus, so the empirical
likelihood is always available. Used by \code{\link[=loglik_empirical_single]{loglik_empirical_single()}}.
}
