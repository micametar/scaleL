% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{impute_origin}
\alias{impute_origin}
\title{Impute a missing scale origin to its posterior mode}
\usage{
impute_origin(
  measures,
  range_prior,
  L_grid,
  origin_grid,
  loglik_fn = get_loglik_fn()
)
}
\arguments{
\item{measures}{Data.frame with \code{mean}, \code{sd}, \code{n}.}

\item{range_prior}{Named numeric vector from
\code{\link[=estimate_range_prior_from_corpus]{estimate_range_prior_from_corpus()}}.}

\item{L_grid}{Candidate L values.}

\item{origin_grid}{Candidate origin values.}

\item{loglik_fn}{Per-measure log-likelihood (default \code{\link[=get_loglik_fn]{get_loglik_fn()}}).
Origin imputation requires the empirical likelihood in the manuscript.}
}
\value{
List with \code{scale_min} (modal origin) and \code{prob}.
}
\description{
Marginalizes the joint range posterior over L and returns the modal origin
and its posterior probability. Resolving the origin to its mode (rather than
propagating it through multiple imputation) keeps the downstream L pipeline
unchanged: the study simply enters it with an imputed \code{scale_min}.
}
