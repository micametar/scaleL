% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/across_study.R
\name{loglik_across_vec}
\alias{loglik_across_vec}
\title{Across-study log-likelihood over the L grid for one target study}
\usage{
loglik_across_vec(measures, mu_hat, tau2_hat, L_grid, scale_min = 1)
}
\arguments{
\item{measures}{Data.frame with \code{mean}, \code{sd}, \code{n} for the target study.}

\item{mu_hat, tau2_hat}{Anchor REML estimates (see \code{\link[=fit_REML]{fit_REML()}}).}

\item{L_grid}{Candidate L values.}

\item{scale_min}{Lower endpoint (default 1).}
}
\value{
Numeric vector of log-likelihoods, one per \code{L_grid} entry.
}
\description{
For each candidate L: \code{theta(L)} and its sampling se come from the target's
own moments; the predictive is \code{N(mu_hat, sqrt(tau2_hat + se(L)^2))}
truncated to \verb{(0, 1)}; the contribution is the log truncated-normal density
of \code{theta(L)}. Candidate L that put \code{theta(L)} outside \verb{(0, 1)} score \code{-Inf},
so the channel also enforces the feasibility floor.
}
