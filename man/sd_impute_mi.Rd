% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sd_impute.R
\name{sd_impute_mi}
\alias{sd_impute_mi}
\title{Multiple imputation of missing SDs on bounded scales (Rubin)}
\usage{
sd_impute_mi(
  d,
  M = 50,
  min_obs = 10,
  group = NULL,
  min_group = 10,
  seed = NULL,
  return_draws = FALSE
)
}
\arguments{
\item{d}{Data.frame as for \code{\link[=sd_feasibility_and_impute]{sd_feasibility_and_impute()}}.}

\item{M}{Number of imputations (default 50).}

\item{min_obs}{Minimum observed SDs to fit the model (default 10).}

\item{group}{Optional moderator column for conditioned fits.}

\item{min_group}{Minimum observed SDs in a level to condition (default 10).}

\item{seed}{Optional seed.}

\item{return_draws}{If \code{TRUE}, also return the \verb{M x n_need} SD draws.}
}
\value{
List with \code{d} (filled \code{sd}, \code{sd_imputed}, \code{sd_impute_var} =
between-imputation variance), \code{need}, and optionally \code{sd_draws}.
}
\description{
Multiply-imputes every missing SD on a known-L bounded scale by Bayesian
linear-regression draws of the dispersion fraction (coefficient AND residual
uncertainty), so the spread of the M imputed SDs reflects genuine predictive
uncertainty. Optionally conditions on a moderator \code{group} with a nested
fallback to the pooled fit (>= \code{min_group} observed SDs in the level).
}
