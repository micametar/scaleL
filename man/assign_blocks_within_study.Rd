% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{assign_blocks_within_study}
\alias{assign_blocks_within_study}
\title{Assign within-study uniformity blocks (internal)}
\usage{
assign_blocks_within_study(
  per_meas_modal_L,
  per_meas_modal_prob,
  min_cluster_size = 2L,
  min_cluster_modal_prob = 0.5
)
}
\arguments{
\item{per_meas_modal_L}{Integer vector, one entry per measure.}

\item{per_meas_modal_prob}{Numeric vector of per-measure modal posterior
probabilities (observed-L measures have probability 1).}

\item{min_cluster_size}{Minimum pooled-block size (default 2L).}

\item{min_cluster_modal_prob}{Minimum per-measure modal probability for
pool eligibility (default 0.5).}
}
\value{
Integer vector of block ids (1..n_blocks), same length as
\code{per_meas_modal_L}.
}
\description{
Given per-measure modal L values and modal probabilities, group eligible
measures (modal probability >= \code{min_cluster_modal_prob}) by their modal L.
Each group of at least \code{min_cluster_size} measures becomes a pooled block.
Remaining measures become singleton blocks (one block per measure).
}
\details{
For backward compatibility, a study with a single measure always yields
exactly one block (a singleton).
}
\keyword{internal}
