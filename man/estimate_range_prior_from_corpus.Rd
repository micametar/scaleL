% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{estimate_range_prior_from_corpus}
\alias{estimate_range_prior_from_corpus}
\title{Estimate a corpus prior over ranges (origin, L)}
\usage{
estimate_range_prior_from_corpus(
  obs_scale_min,
  obs_L,
  origin_grid,
  L_grid,
  alpha = 0.5,
  base_L_prior = NULL
)
}
\arguments{
\item{obs_scale_min}{Observed lower endpoints (NA where missing).}

\item{obs_L}{Observed L values (NA where missing).}

\item{origin_grid}{Candidate origin values.}

\item{L_grid}{Candidate L values.}

\item{alpha}{Laplace pseudocount (default 0.5).}

\item{base_L_prior}{Optional named L prior used for the flat-origin
fallback.}
}
\value{
Named numeric vector indexed by \code{"origin|L"}, summing to 1.
}
\description{
Mirrors \code{\link[=estimate_prior_from_corpus]{estimate_prior_from_corpus()}} but the unit is the full range
\verb{(a, L)} (lower endpoint and length). Laplace-smoothed over the candidate
origin x L grid. If no study reports an origin, falls back to a flat origin
prior times the supplied L prior so the machinery still runs.
}
