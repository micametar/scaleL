% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sd_impute.R
\name{impute_reliability}
\alias{impute_reliability}
\title{Impute mean inter-item correlation with a reliability cascade}
\usage{
impute_reliability(d, group = "dimension", default_rbar = 0.25)
}
\arguments{
\item{d}{Data.frame with \code{alpha}, \code{items}, \code{n}, and the \code{group} column.}

\item{group}{Column naming the field-level fallback stratum (default
\code{"dimension"}).}

\item{default_rbar}{Default \code{rbar} when reliability is unreported (0.25).}
}
\value{
\code{d} with added columns \code{alpha_filled}, \code{rbar_source}, \code{rbar}.
}
\description{
Fills the per-row mean inter-item correlation \code{rbar} using a reported ->
field -> global -> default cascade. The correction factor
\verb{J / (1 + (J - 1) rbar)} is decreasing in \code{rbar}, so under-correction is the
damaging direction; uncertain \code{rbar} is shifted DOWN (toward more inflation).
}
