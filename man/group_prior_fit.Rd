% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/across_study.R
\name{group_prior_fit}
\alias{group_prior_fit}
\title{Fit the across-study group prior with optional moderator conditioning}
\usage{
group_prior_fit(
  anc,
  target_level = NA,
  mode = c("marginal", "conditioned"),
  min_k = 10L
)
}
\arguments{
\item{anc}{Anchor table from \code{\link[=anchor_table]{anchor_table()}}.}

\item{target_level}{Moderator level of the target study.}

\item{mode}{\code{"marginal"} (pool all anchors) or \code{"conditioned"} (use the
target's level if it has >= \code{min_k} anchors, else fall back to pooled).}

\item{min_k}{Minimum anchors in a level to condition (default 10).}
}
\value{
List with \code{mu}, \code{tau2}, \code{used} (\code{"pooled"}/\code{"conditioned"}), \code{k};
or \code{NULL} if fewer than two usable anchors.
}
\description{
Fit the across-study group prior with optional moderator conditioning
}
